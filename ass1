\\Write a c program to print factors of a given number 
#include <stdio.h>
main()
{
    int n,i=1;
    printf("enter a value to get factors");
    scanf("%d",&n);
    while(i<=n)
    {
        if(n%i==0)
        {
            printf("%d \n",i);
        }
        i++;
    }
}
