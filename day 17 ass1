#include <stdio.h>

int main()
{
    int i,n,ar[10],lowest;
    printf("Enter number of elements:\n");
    scanf("%d",&n);
    printf("Enter the elements \n");
    for(i=0;i<n;i++)
    {
        scanf("%d",&ar[10]);
        
    }
    small=a[0];
    for (i=1;i<n;i++)
    {
        if(a[i]<small)
        {
            small=a[i];
        }
    }
    printf("Lowest of %d elements = %d",n,small);

    return 0;
}
